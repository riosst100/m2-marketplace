<?php
/**
 * Landofcoder
 *
 * NOTICE OF LICENSE
 *
 * This source file is subject to the Landofcoder.com license that is
 * available through the world-wide-web at this URL:
 * https://landofcoder.com/terms
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade this extension to newer
 * version in the future.
 *
 * @category   Landofcoder
 * @package    Lof_MarketPlace
 * @copyright  Copyright (c) 2021 Landofcoder (https://www.landofcoder.com/)
 * @license    https://landofcoder.com/terms
 */

// phpcs:disable Magento2.Templates.ThisInTemplate
// phpcs:disable Magento2.Security.XssTemplate.FoundNotAllowed
// phpcs:disable Generic.Files.LineLength.TooLong
/**
 * @var $block Lof\MarketPlace\Block\Seller\Message\View
 */
$helper = $this->helper(\Lof\MarketPlace\Helper\Data::class);
$formActionUrl = $this->getUrl('catalog/seller/savemsg');
$currPageUrl = $helper->getCurrentUrls();
$this->isRead();
?>
<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2><?= $block->escapeHtml($this->getMessage()->getSubject()) ?></h2>
                <div class="clearfix"></div>
            </div>
            <div class="x_content seller-message-meta">
                <table class="tile_info">
                    <tbody>
                    <tr>
                        <td>
                            <b><?= $block->escapeHtml(__('Customer Name')) ?></b>
                        </td>
                        <td><p><?= $block->escapeHtml($this->getMessage()->getSenderName()) ?></p></td>
                    </tr>
                    <tr>
                        <td>
                            <b><?= $block->escapeHtml(__('Customer Email')) ?></b>
                        </td>
                        <td><p><?= $block->escapeHtml($block->getMessage()->getSenderEmail()) ?></p></td>
                    </tr>
                    <tr>
                        <td>
                            <b><?= $block->escapeHtml(__('Status')) ?></b>
                        </td>
                        <td><p><?= $block->escapeHtml($helper->getStatus($block->getMessage()->getStatus())) ?></p></td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <?php if ($block->getMessage()->getStatus() != 0): ?>
        <div class="col-md-6 col-sm-6 col-xs-6">
            <div class="x_panel">
                <div class="x_title">
                    <h2><?= $block->escapeHtml(__('Post a Reply')) ?></h2>
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                      <form id="form-message" data-parsley-validate=""
                          action="<?= $block->escapeHtmlAttr($formActionUrl) ?>"
                          class="form-horizontal form-label-left seller-message-reply" method="post"
                          data-hasrequired="<?= $block->escapeHtmlAttr(__('* Required Fields')) ?>"
                          data-mage-init='{"validation":{}}'>
                        <div class="form-group">
                            <label class="control-label col-md-2 col-sm-2 col-xs-2"><?= __('Status'); ?></label>
                            <div class="col-md-10 col-sm-10 col-xs-10">
                                <select class="form-control" id="status" name="status">
                                    <?php foreach ($helper->arrayStatus() as $status): ?>
                                        <option <?= ($status['value'] == $block->getMessage()->getStatus()) ? 'selected' : '' ?>
                                            value="<?= $status['value']; ?>">
                                            <?= $block->escapeHtml($status['label']) ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-md-2 col-sm-2 col-xs-2" for="content">
                                <?= $block->escapeHtml(__('Message')) ?>
                                <span class="required">*</span>
                            </label>
                            <div class="col-md-10 col-sm-10 col-xs-10">
                                <textarea name="content" rows="10" id="content" required="required"
                                          class="form-control col-md-7 col-xs-12"> </textarea>
                            </div>
                        </div>
                        <div class="ln_solid"></div>
                        <div class="form-group action-button">
                            <div class="col-md-12 col-sm-12 col-xs-12">
                                <input type="hidden" name="sender_id"
                                       value="<?= $block->escapeHtmlAttr($block->getSeller()->getSellerId()) ?>"/>
                                <input type="hidden" name="seller_send" value="1"/>
                                <input type="hidden" name="sender_name"
                                       value="<?= $block->escapeHtmlAttr($block->getSeller()->getName()) ?>"/>
                                <input type="hidden" name="sender_email"
                                       value="<?= $block->escapeHtmlAttr($block->getSeller()->getEmail()) ?>"/>
                                <input type="hidden" name="receiver_id"
                                       value="<?= $block->escapeHtmlAttr($block->getMessage()->getSenderId()) ?>"/>
                                <input type="hidden" name="receiver_name"
                                       value="<?= $block->escapeHtmlAttr($block->getMessage()->getSenderName()) ?>"/>
                                <input type="hidden" name="receiver_email"
                                       value="<?= $block->escapeHtmlAttr($block->getMessage()->getSenderEmail()) ?>"/>
                                <input type="hidden" name="message_id"
                                       value="<?= $block->escapeHtmlAttr($block->getMessage()->getMessageId()) ?>"/>
                                <input type="hidden" name="currUrl" id="currUrl"
                                       value="<?= $block->escapeHtmlAttr($currPageUrl) ?>"/>
                                <button type="submit" class="btn btn-success">
                                    <?= $block->escapeHtml(__('Submit')) ?>
                                </button>
                                <button class="btn btn-primary" type="reset">
                                    <?= $block->escapeHtml(__('Reset')) ?>
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    <?php endif; ?>
    <div class="col-md-6 col-sm-6 col-xs-6">
        <div class="x_panel">
            <div class="x_title">
                <h2><?= $block->escapeHtml(__('Message History')) ?></h2>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <ul class="chat-messages" id="seller-message-list">
                    <script>
                        (function(){
                            var list = document.getElementById('seller-message-list');
                            if (list) { list.scrollTop = list.scrollHeight; }
                        })();
                    </script>
                    <?php foreach ($block->getDetail() as $detail):
                        $isSelf = ($detail->getSenderId() == $block->getSeller()->getSellerId());
                        $timeText = $helper->formatDateTime($detail->getCreatedAt(), \IntlDateFormatter::SHORT, true);
                    ?>
                        <li class="chat-row <?= $isSelf ? 'self' : 'other' ?>">
                            <div class="chat-bubble <?= $isSelf ? 'self' : 'other' ?>">
                                <?php if ($isSelf): ?>
                                    <div class="chat-header self">
                                        <span class="chat-time"><?= $block->escapeHtml($timeText) ?></span>
                                    </div>
                                    <div class="chat-content"><?= $block->escapeHtml($detail->getContent()) ?></div>
                                <?php else: ?>
                                    <div class="chat-header">
                                        <span class="chat-sender"><?= $block->escapeHtml($detail->getSenderName()) ?></span>
                                        <span class="chat-time"><?= $block->escapeHtml($timeText) ?></span>
                                    </div>
                                    <div class="chat-content"><?= $block->escapeHtml($detail->getContent()) ?></div>
                                <?php endif; ?>
                            </div>
                        </li>
                    <?php endforeach; ?>
                    <?php
                        $baseIsSelf = ($block->getMessage()->getSenderId() == $block->getSeller()->getSellerId());
                        $baseTimeText = $helper->formatDateTime($block->getMessage()->getCreatedAt(), \IntlDateFormatter::SHORT, true);
                    ?>
                    <li class="chat-row <?= $baseIsSelf ? 'self' : 'other' ?>">
                        <div class="chat-bubble <?= $baseIsSelf ? 'self' : 'other' ?>">
                            <?php if ($baseIsSelf): ?>
                                <div class="chat-header self">
                                    <span class="chat-time"><?= $block->escapeHtml($baseTimeText) ?></span>
                                </div>
                                <div class="chat-content"><?= $block->escapeHtml($block->getMessage()->getDescription()) ?></div>
                            <?php else: ?>
                                <div class="chat-header">
                                    <span class="chat-sender"><?= $block->escapeHtml($block->getMessage()->getSenderName()) ?></span>
                                    <span class="chat-time"><?= $block->escapeHtml($baseTimeText) ?></span>
                                </div>
                                <div class="chat-content"><?= $block->escapeHtml($block->getMessage()->getDescription()) ?></div>
                            <?php endif; ?>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>
